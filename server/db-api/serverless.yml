# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org: akbrauer
# "app" enables Serverless Framework Dashboard features and sharing them with other Services.
app: recipe-archive
# "service" is the name of this project. This will also be added to your AWS resource names.
service: db-api

build:
  esbuild:
    external:
      - sharp

package:
  individually: true

provider:
  name: aws
  runtime: nodejs20.x
  environment:
    # Removed quotations from all pg vars
    PGHOST: ep-blue-union-abefnl10-pooler.eu-west-2.aws.neon.tech
    PGDATABASE: recipe_archive
    PGUSER: recipe_archive_owner
    PGPASSWORD: npg_ng4deQmOAPh8
    CLOUDINARY_CLOUD_NAME: dviijvxfy
    CLOUDINARY_KEY: 184525966761295
    CLOUDINARY_SECRET: OAub7jXA7K_qQ_IIhmiuw7T2RMk
  httpApi:
    cors: true

functions:
  api:
    handler: handler.handler
    events:
      - httpApi: '*'
    timeout: 20
    package:
      patterns:
        - 'node_modules/sharp'
        - 'node_modules/@img'